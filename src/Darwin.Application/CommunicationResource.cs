namespace Darwin.Application
{
    /// <summary>
    /// Marker type for shared application-layer communication resources.
    /// Hosts can resolve localized transactional fallback content without depending on UI-layer resources.
    /// </summary>
    public sealed class CommunicationResource
    {
    }
}
