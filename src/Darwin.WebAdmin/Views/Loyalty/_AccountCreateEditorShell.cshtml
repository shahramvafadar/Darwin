@model Darwin.WebAdmin.ViewModels.Loyalty.CreateLoyaltyAccountVm
@{
    ViewData["Title"] = "Create Loyalty Account";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="mb-1">Create Loyalty Account</h1>
        <div class="text-muted">Provision a loyalty account for a business member who has not self-enrolled yet.</div>
    </div>
</div>

<partial name="_AccountCreateForm" model="Model" />
