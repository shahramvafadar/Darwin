@model Darwin.WebAdmin.ViewModels.Inventory.StockTransfersListVm
@inject Darwin.WebAdmin.Localization.IAdminTextLocalizer T
@{
    ViewData["Title"] = T.T("StockTransfersTitle");
    var inventoryCulture = System.Globalization.CultureInfo.CurrentCulture;
    string LocalizeStockTransferStatus(object? status) => status is null ? "-" : T.T(status.ToString() ?? string.Empty);
    string InventoryDateTimeText(DateTime utc) => utc.ToString("g", inventoryCulture);
    string StockTransferOpsHealthBadgeClass(bool healthy) => healthy
        ? "badge bg-success-subtle text-success-emphasis"
        : "badge bg-warning-subtle text-warning-emphasis";
    string StockTransferOpsHealthBadgeText(bool healthy) => healthy ? T.T("Ready") : T.T("NeedsAttention");
    var stockTransferAttentionCount = Model.Summary.DraftCount + Model.Summary.InTransitCount;
}

<div id="inventory-stock-transfers-workspace-shell">
<h1 class="mb-3">@T.T("StockTransfersTitle")</h1>

<div class="d-flex justify-content-between align-items-center mb-3">
    <form asp-action="StockTransfers" method="get" class="d-flex gap-2"
          hx-get="@Url.Action("StockTransfers", "Inventory")"
          hx-target="#inventory-stock-transfers-workspace-shell"
          hx-swap="outerHTML"
          hx-push-url="true">
        <input type="hidden" name="businessId" value="@ViewBag.BusinessId" />
        <select name="warehouseId" asp-items="Model.WarehouseOptions" class="form-select"></select>
        <input type="text" name="q" value="@Model.Query" class="form-control" placeholder="@T.T("SearchTransfersPlaceholder")">
        <select name="filter" asp-items="Model.FilterItems" class="form-select"></select>
        <button class="btn btn-outline-secondary" type="submit">@T.T("Filter")</button>
        <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-outline-secondary"
           hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
           hx-target="#inventory-stock-transfers-workspace-shell"
           hx-swap="outerHTML"
           hx-push-url="true">@T.T("Reset")</a>
    </form>
    <a asp-action="CreateStockTransfer" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-primary"
       hx-get="@Url.Action("CreateStockTransfer", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
       hx-target="#inventory-stock-transfers-workspace-shell"
       hx-swap="outerHTML"
       hx-push-url="true">@T.T("NewTransfer")</a>
</div>

<div class="d-flex gap-2 flex-wrap mb-3">
    <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="Draft" class="btn btn-sm @(Model.Filter == Darwin.Application.Inventory.DTOs.StockTransferQueueFilter.Draft ? "btn-outline-secondary" : "btn-outline-dark")"
       hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "Draft" })"
       hx-target="#inventory-stock-transfers-workspace-shell"
       hx-swap="outerHTML"
       hx-push-url="true">@T.T("Draft")</a>
    <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="InTransit" class="btn btn-sm @(Model.Filter == Darwin.Application.Inventory.DTOs.StockTransferQueueFilter.InTransit ? "btn-warning" : "btn-outline-warning")"
       hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "InTransit" })"
       hx-target="#inventory-stock-transfers-workspace-shell"
       hx-swap="outerHTML"
       hx-push-url="true">@T.T("InTransit")</a>
    <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="Completed" class="btn btn-sm @(Model.Filter == Darwin.Application.Inventory.DTOs.StockTransferQueueFilter.Completed ? "btn-success" : "btn-outline-success")"
       hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "Completed" })"
       hx-target="#inventory-stock-transfers-workspace-shell"
       hx-swap="outerHTML"
       hx-push-url="true">@T.T("Completed")</a>
    <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-secondary"
       hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query })"
       hx-target="#inventory-stock-transfers-workspace-shell"
       hx-swap="outerHTML"
       hx-push-url="true">@T.T("ClearQueueFilters")</a>
</div>

<div class="row g-3 mb-4">
    <div class="col-md-3">
        <div class="card h-100">
            <div class="card-body">
                <div class="text-muted small">@T.T("Total")</div>
                <div class="fs-4 fw-semibold">@Model.Summary.TotalCount</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-secondary-subtle">
            <div class="card-body">
                <div class="text-muted small">@T.T("Draft")</div>
                <div class="fs-4 fw-semibold">@Model.Summary.DraftCount</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-warning-subtle">
            <div class="card-body">
                <div class="text-muted small">@T.T("InTransit")</div>
                <div class="fs-4 fw-semibold">@Model.Summary.InTransitCount</div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-success-subtle">
            <div class="card-body">
                <div class="text-muted small">@T.T("Completed")</div>
                <div class="fs-4 fw-semibold">@Model.Summary.CompletedCount</div>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm mb-4">
    <div class="card-body">
        <div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-3">
            <div>
                <div class="d-flex flex-wrap align-items-center gap-2">
                    <h2 class="h5 mb-0">@T.T("GoLiveReadiness")</h2>
                    <span class="@StockTransferOpsHealthBadgeClass(stockTransferAttentionCount == 0)">@StockTransferOpsHealthBadgeText(stockTransferAttentionCount == 0)</span>
                </div>
                <p class="text-muted small mb-0 mt-2">
                    @T.T("StockTransferOpsPlaybooks")
                    @if (stockTransferAttentionCount > 0)
                    {
                        <span class="ms-1">@T.T("NeedsAttention"): @stockTransferAttentionCount</span>
                    }
                </p>
            </div>
            <div class="d-flex flex-wrap gap-2">
                <a asp-action="CreateStockTransfer" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-primary"
                   hx-get="@Url.Action("CreateStockTransfer", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                   hx-target="#inventory-stock-transfers-workspace-shell"
                   hx-swap="outerHTML"
                   hx-push-url="true">@T.T("NewTransfer")</a>
                <a asp-action="StockLevels" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-secondary"
                   hx-get="@Url.Action("StockLevels", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                   hx-target="#inventory-stock-transfers-workspace-shell"
                   hx-swap="outerHTML"
                   hx-push-url="true">@T.T("StockLevels")</a>
            </div>
        </div>

        <div class="row g-3">
            <div class="col-md-6 col-xl-3">
                <div class="border rounded p-3 h-100">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-semibold">@T.T("Draft")</div>
                            <div class="small text-muted mt-1">@T.T("NeedsAttention")</div>
                        </div>
                        <span class="@StockTransferOpsHealthBadgeClass(Model.Summary.DraftCount == 0)">@Model.Summary.DraftCount</span>
                    </div>
                    <div class="small text-muted mt-3">@T.T("InventoryTransfersPlaybookDraftAction")</div>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="Draft" class="btn btn-sm btn-outline-dark"
                           hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "Draft" })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("Draft")</a>
                        <a asp-action="CreateStockTransfer" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-primary"
                           hx-get="@Url.Action("CreateStockTransfer", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("NewTransfer")</a>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-xl-3">
                <div class="border rounded p-3 h-100">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-semibold">@T.T("InTransit")</div>
                            <div class="small text-muted mt-1">@T.T("NeedsAttention")</div>
                        </div>
                        <span class="@StockTransferOpsHealthBadgeClass(Model.Summary.InTransitCount == 0)">@Model.Summary.InTransitCount</span>
                    </div>
                    <div class="small text-muted mt-3">@T.T("InventoryTransfersPlaybookInTransitAction")</div>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="InTransit" class="btn btn-sm btn-outline-warning"
                           hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "InTransit" })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("InTransit")</a>
                        <a asp-action="StockLevels" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-secondary"
                           hx-get="@Url.Action("StockLevels", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("StockLevels")</a>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-xl-3">
                <div class="border rounded p-3 h-100">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-semibold">@T.T("Completed")</div>
                            <div class="small text-muted mt-1">@T.T("Queue")</div>
                        </div>
                        <span class="@StockTransferOpsHealthBadgeClass(Model.Summary.CompletedCount > 0)">@Model.Summary.CompletedCount</span>
                    </div>
                    <div class="small text-muted mt-3">@T.T("InventoryTransfersPlaybookInTransitScope")</div>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-filter="Completed" class="btn btn-sm btn-outline-success"
                           hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query, filter = "Completed" })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("Completed")</a>
                        <a asp-action="StockLevels" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-secondary"
                           hx-get="@Url.Action("StockLevels", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("StockLevels")</a>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-xl-3">
                <div class="border rounded p-3 h-100">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-semibold">@T.T("StockTransfersTitle")</div>
                            <div class="small text-muted mt-1">@T.T("StockLevels")</div>
                        </div>
                        <span class="@StockTransferOpsHealthBadgeClass(Model.Summary.TotalCount == 0)">@Model.Summary.TotalCount</span>
                    </div>
                    <div class="small text-muted mt-3">@T.T("InventoryTransfersPlaybookDraftScope")</div>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <a asp-action="StockLevels" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-secondary"
                           hx-get="@Url.Action("StockLevels", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("StockLevels")</a>
                        <a asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" class="btn btn-sm btn-outline-primary"
                           hx-get="@Url.Action("StockTransfers", "Inventory", new { businessId = ViewBag.BusinessId, warehouseId = Model.WarehouseId, q = Model.Query })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("StockTransfersTitle")</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@if (Model.Playbooks.Count > 0)
{
    <div class="card mb-4">
        <div class="card-header">@T.T("StockTransferOpsPlaybooks")</div>
        <div class="table-responsive">
            <table class="table table-sm mb-0 align-middle">
                <thead>
                <tr>
                    <th>@T.T("Playbook")</th>
                    <th>@T.T("WhenItApplies")</th>
                    <th>@T.T("OperatorAction")</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var playbook in Model.Playbooks)
                {
                    <tr>
                        <td class="fw-semibold">@playbook.Title</td>
                        <td>@playbook.ScopeNote</td>
                        <td>@playbook.OperatorAction</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
}

<div class="table-responsive">
    <table class="table table-sm table-striped align-middle">
        <thead><tr><th>@T.T("From")</th><th>@T.T("To")</th><th>@T.T("Status")</th><th>@T.T("Lines")</th><th>@T.T("Created")</th><th class="text-end">@T.T("Actions")</th></tr></thead>
        <tbody>
        @if (Model.Items.Count == 0)
        {
            <tr><td colspan="6" class="text-center text-muted">@T.T("NoStockTransfersFound")</td></tr>
        }
        else
        {
            foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.FromWarehouseName</td>
                    <td>@item.ToWarehouseName</td>
                    <td>
                        @if (string.Equals(item.Status, "InTransit", StringComparison.OrdinalIgnoreCase))
                        {
                            <span class="badge text-bg-warning">@T.T("InTransit")</span>
                        }
                        else if (string.Equals(item.Status, "Completed", StringComparison.OrdinalIgnoreCase))
                        {
                            <span class="badge text-bg-success">@T.T("Completed")</span>
                        }
                        else
                        {
                            <span class="badge text-bg-secondary">@LocalizeStockTransferStatus(item.Status)</span>
                        }
                    </td>
                    <td>@item.LineCount</td>
                    <td>@InventoryDateTimeText(item.CreatedAtUtc)</td>
                    <td class="text-end"><a asp-action="EditStockTransfer" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary"
                           hx-get="@Url.Action("EditStockTransfer", "Inventory", new { id = item.Id })"
                           hx-target="#inventory-stock-transfers-workspace-shell"
                           hx-swap="outerHTML"
                           hx-push-url="true">@T.T("Edit")</a></td>
                </tr>
            }
        }
        </tbody>
    </table>
</div>

<pager page="Model.Page" page-size="Model.PageSize" total="Model.Total" asp-controller="Inventory" asp-action="StockTransfers" asp-route-businessId="@ViewBag.BusinessId" asp-route-warehouseId="@Model.WarehouseId" asp-route-q="@Model.Query" asp-route-filter="@Model.Filter" hx-target="#inventory-stock-transfers-workspace-shell" hx-swap="outerHTML" hx-push-url="true"></pager>
</div>
