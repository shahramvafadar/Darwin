namespace Darwin.Web.Security
{
    /// <summary>
    /// Well-known permission keys used across the Web layer.
    /// Must match Domain/seed values.
    /// </summary>
    public static class PermissionKeys
    {
        public const string FullAdminAccess = "FullAdminAccess";
        public const string AccessMemberArea = "AccessMemberArea";
    }
}
