namespace Darwin.Mobile.Business;

/// <summary>
/// Provides a localized placeholder page for designer and tooling scenarios.
/// The business application uses the shell as the primary runtime entry point.
/// </summary>
public partial class MainPage : ContentPage
{
    /// <summary>
    /// Initializes the placeholder page used outside the shell-driven navigation flow.
    /// </summary>
    public MainPage()
    {
        InitializeComponent();
    }
}
