<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:buttons="clr-namespace:Syncfusion.Maui.Toolkit.Buttons;assembly=Syncfusion.Maui.Toolkit"
    xmlns:vm="clr-namespace:Darwin.Mobile.Business.ViewModels"
    xmlns:res="clr-namespace:Darwin.Mobile.Business.Resources"
    x:Class="Darwin.Mobile.Business.Views.StaffAccessBadgePage"
    x:DataType="vm:StaffAccessBadgeViewModel"
    Title="{x:Static res:AppResources.StaffAccessBadgeTitle}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="14">
            <Label Text="{x:Static res:AppResources.StaffAccessBadgeTitle}"
                   Style="{StaticResource TitleLabelStyle}" />

            <Label Text="{x:Static res:AppResources.StaffAccessBadgeDescription}"
                   Style="{StaticResource SecondaryBodyLabelStyle}" />

            <Border Style="{StaticResource CardBorderStyle}" Padding="16">
                <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                    <Image Source="{Binding BadgeImage}"
                           HeightRequest="240"
                           WidthRequest="240"
                           Aspect="AspectFit" />

                    <Label Text="{Binding BadgeSummary}"
                           HorizontalTextAlignment="Center"
                           Style="{StaticResource BodyLabelStyle}"
                           FontAttributes="Bold" />

                    <Label Text="{Binding OperatorRoleDisplay}"
                           Style="{StaticResource SecondaryBodyLabelStyle}"
                           LineBreakMode="WordWrap"
                           HorizontalTextAlignment="Center" />

                    <Label Text="{Binding ExpiresInText}"
                           Style="{StaticResource CaptionLabelStyle}"
                           LineBreakMode="WordWrap"
                           HorizontalTextAlignment="Center" />

                    <Label Text="{Binding ErrorMessage}"
                           Style="{StaticResource ErrorLabelStyle}"
                           IsVisible="{Binding HasError}"
                           HorizontalTextAlignment="Center" />

                    <buttons:SfButton Text="{x:Static res:AppResources.StaffAccessBadgeRefreshButton}"
                                      Style="{StaticResource SyncfusionPrimaryButtonStyle}"
                                      Command="{Binding RefreshBadgeCommand}" />

                    <ActivityIndicator IsVisible="{Binding IsBusy}"
                                       IsRunning="{Binding IsBusy}"
                                       Color="{StaticResource BrandGold500}" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
